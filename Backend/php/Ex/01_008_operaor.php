<?
// 1. 산술 연산자
    // echo "더하기 : 1 + 1 = ", 1 + 1, "\n";
    // echo "빼기 : 1 - 1 = ", 1 - 1, "\n";
    // echo "곱하기 : 2 * 2 = ", 2 * 2, "\n";
    // echo "나누기 : 6 / 2 = ", 6 / 2,"\n";
    // echo (10 - 5) * 8 / 10,"\n" ;

// 2. 증가 / 감소 (증감) 연산자
    // $num1 = 8;
    // $num1++;
    // echo $num1;

    // echo "\n";

    // $num2 = 8;
    // $num2--;
    // echo $num2;

    // echo "\n";

    // $num1 = 8;
    // ++$num1;
    // echo $num1;

    // echo "\n";

    // $num2 = 8;
    // --$num2;
    // echo $num2;

    // echo "\n";

    // echo "나머지 : 2 % 3 = ", 2 % 3;

    // 3. 산술 대입 연산자
    // $num = 5;
    // $num = $num + 2;
    // $num += 2;
    // $num -= 2;
    // $num *= 2;
    // $num /= 2;
    // $num = 5;
    // $num %= 6;

    // echo $num, "\n";

    // 산술 연산자를 이용해서 계산
    // $tng_num = 10;

    // $tng_num += 10;
    // echo $tng_num, "\n";

    // $tng_num /= 5;
    // echo $tng_num, "\n";

    // $tng_num -= 4;
    // echo $tng_num, "\n";

    // $tng_num %= 7;
    // echo $tng_num, "\n";

    // $tng_num *= 3;
    // echo $tng_num, "\n";

    // 4. 비교 연산자
    // var_dump( 1 > 0 );
    // var_dump( 1 < 0 );
    // var_dump( 1 >= 0 );
    // var_dump( 1 <= 0 );

    // $num = 1;
    // $str = '1';
    // var_dump($num == $str); // 값의 형태만 비교 | int와 str이 숫자와 문자지만 형태만 비교하기 때문에 true
    // var_dump($num === $str); // 값의 데이터 타입까지 비교 | false
    // var_dump($num != $str); // 값의 형태만 비교 | false
    // var_dump($num !== $str); // 값의 데이터 타입까지 비교 | true
    // 되도록이면 === / !==인 완전 비교를 사용 


    // 5. 논리 연산자
    // and 연산자 & 두 조건을 모두 만족 했을 때
    // var_dump( 1 === 1 && 2 === 2);
    // var_dump( 1 === 1 && 2 === 1);

    // // or 연산자 | 하나의 조건이라도 만족 했을 때
    // var_dump( 1 === 1 || 2 === 2);
    // var_dump( 1 === 2 || 2 === 1);

    // // not 연산자
    // var_dump( !(1 === 1) ); //해당 비교연산자는 해당 1 === 1인 값을 !()로 거짓으로 만들어줌
    // var_dump( 1 != 1 ); // 해당 부분은 1 = 1면 true인데 !=으로 틀리다라는 조건을 줬기 때문에 결과값은 false가 나옴

     
?>
