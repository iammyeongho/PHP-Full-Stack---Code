<?

    $arr = [1, 2, 3, 4, 5];
    // echo count($arr);

    // for문을 이용해 배열 값을 출력 | count에 배열의 수를 계산하는데 수를 계산하기 때문에 1부터 시작해서 1개가 더 많기 때문에 -1을 넣어줘야함
    // for($i = 0; $i <= count($arr) - 1; $i++)
    // {
    //     echo $arr[$i];
    // }

    // foreach : 배열의 길이만큼 루프하는 문법
    // arr은 루프할 배열, kay는 키, val는 값

    // foreach($arr as $kay => $val)
    // {
    //     echo "{$kay} : {$val} \n";
    // }

    // $arr2 = 
    // [
    //     "A" => "AAA"
    //     ,"B" => "BBB"
    //     ,"C" => "CCC"
    // ];

    // 키 값이 필요할 때
    // foreach($arr2 as $kay => $val)
    // {
    //     echo "{$kay} : {$val} \n";
    // }

    // 키 값이 필요 없을 시
    // foreach($arr2 as $val)
    // {
    //     echo "{$val} \n";
    // }

?>